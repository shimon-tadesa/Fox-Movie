{
  "name": "final-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-client": "cd my-project && npm install && npm run build",
    "deploy": "cd my-project  && npm run deploy-to-server",
    "build-server": "cd server && npm install",
    "start": "cd server && node server.js",
    "postinstall": "npm run build-client && npm run deploy && npm run build-server"
  },
  "author": "",
  "license": "ISC"
}
